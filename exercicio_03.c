// Idade expressa em anos, meses e dias
#include <stdio.h>

int main() {
    int anos = 18, meses = 4, dias = 12;

    printf("Idade: %d anos, %d meses, %d dias\n", anos, meses, dias);

    return 0;
}
