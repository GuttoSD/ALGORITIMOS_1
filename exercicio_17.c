//Considere um algoritmo que exiba os lados de um triângulo.
#include <stdio.h>

int main() {
    float lado1 = 3.0, lado2 = 4.0, lado3 = 5.0;

    printf("Lados do triângulo: %.1f, %.1f, %.1f\n", lado1, lado2, lado3);

    return 0;
}
